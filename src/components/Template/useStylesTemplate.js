import { createUseStyles } from "react-jss";

const useStylesTemplate = createUseStyles({
  card_topic: {
    fontSize: "15px",
    fontWeight: 600,
    marginBottom: "15px",
    color: "#f8f9fa",
  },
});

export default useStylesTemplate;
