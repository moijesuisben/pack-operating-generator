import { createUseStyles } from "react-jss";

const useStylesCards = createUseStyles({
  cards_container: {
    display: "flex",
    flexDirection: "row",
    justifyContent: "center",
    margin: "0 7vw",
  },
});

export default useStylesCards;
